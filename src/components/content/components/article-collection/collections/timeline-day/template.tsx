import styles from "./template.module.scss";

// Uses scroller so that template gets used here
// No actual templating required
export const TimelineDayTemplate = () => {
	return <div className={styles.template} />;
};
